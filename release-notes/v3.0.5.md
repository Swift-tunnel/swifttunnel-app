## What's changed

- Fixes updates failing with "network resource unavailable". Stuck installs now repair themselves.
- The session timer shows real connected time instead of counting through drops.
- Errors show one clear line instead of a page of raw HTML.

Release: https://github.com/Swift-tunnel/swifttunnel-app/releases/tag/v3.0.5
