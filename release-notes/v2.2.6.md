## What's changed

· Redesigned Optimize tab: each tier is now a page of cards you flip through with ‹ › arrows, with a smooth slide animation. Every card shows its category, caution/restart badges, and exactly what it changes.

· 8 new optimizations and a new Expert tier! all real(no placebo) and fully reversible.

· Fixed internet-cafe PCs freezing or dying with SwiftTunnel. Diskless cafe setups (very common in Vietnam) run Windows from the network, and two things could starve their "disk": connecting now waves the cafe's boot traffic through at the driver level so it never touches the app, and RAM cleaning automatically turns itself off on those machines.

· Fixed Bypass Country Ban for fully-blocked countries (e.g. Egypt): game assets were still loading over your direct connection, which is blocked there. Full bypass now relays everything, including textures and assets.

· Lighter on low-end PCs: the FPS tracer only runs while the in-game overlay is enabled, and background process scans run far less often.

**Release**: https://github.com/Swift-tunnel/swifttunnel-app/releases/tag/v2.2.6
