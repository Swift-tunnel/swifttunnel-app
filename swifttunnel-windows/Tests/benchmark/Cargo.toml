[package]
name = "vpn-benchmark"
version = "2.0.0"
edition = "2021"
description = "VPN Benchmark: Real CPU usage comparison - SwiftTunnel vs WireGuard vs WireSock"

[dependencies]
# System info for CPU/RAM monitoring
sysinfo = "0.32"

# Fast mutex for CPU monitoring thread
parking_lot = "0.12"
