workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "web"

stages:
  - mirror

mirror_notice:
  stage: mirror
  script:
    - echo "GitHub is the source of truth for SwiftTunnel releases."
    - echo "This GitLab project is kept as a read-only mirror only."
